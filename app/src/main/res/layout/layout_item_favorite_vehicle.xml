<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_lg"
    app:cardBackgroundColor="@color/bg_card"
    app:cardCornerRadius="@dimen/radius_xl"
    app:cardElevation="0dp"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="@dimen/favorite_vehicle_image_height"
        android:padding="@dimen/spacing_lg">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardVehicleImage"
            android:layout_width="@dimen/favorite_vehicle_image_width"
            android:layout_height="@dimen/favorite_vehicle_image_height"
            android:layout_marginEnd="@dimen/spacing_lg"
            app:cardBackgroundColor="@color/bg_card"
            app:cardCornerRadius="@dimen/radius_lg"
            app:cardElevation="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/layoutVehicleInfo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:strokeWidth="0dp">

            <ImageView
                android:id="@+id/ivVehicle"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_directions_car" />

        </com.google.android.material.card.MaterialCardView>

        <LinearLayout
            android:id="@+id/layoutVehicleInfo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/spacing_lg"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="@id/cardVehicleImage"
            app:layout_constraintEnd_toStartOf="@id/cardFav"
            app:layout_constraintStart_toEndOf="@id/cardVehicleImage"
            app:layout_constraintTop_toTopOf="@id/cardVehicleImage">

            <TextView
                android:id="@+id/tvVehicleName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="@font/inter_bold"
                android:maxLines="1"
                android:text="@string/vehicle_name_placeholder"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/text_lg"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvVehicleType"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_2xs"
                android:ellipsize="end"
                android:fontFamily="@font/inter_medium"
                android:maxLines="2"
                android:text="@string/feature_single_placeholder"
                android:textColor="@color/text_hint"
                android:textSize="@dimen/text_sm"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvVehiclePrice"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_2xs"
                android:ellipsize="end"
                android:fontFamily="@font/inter_bold"
                android:maxLines="1"
                android:text="€0"
                android:textColor="@color/primary_green_dark"
                android:textSize="@dimen/text_lg"
                android:textStyle="bold" />

        </LinearLayout>

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardFav"
            android:layout_width="@dimen/favorite_vehicle_fav_size"
            android:layout_height="@dimen/favorite_vehicle_fav_size"
            app:cardBackgroundColor="@color/primary_green_light"
            app:cardCornerRadius="@dimen/radius_md"
            app:cardElevation="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:strokeWidth="0dp">

            <ImageView
                android:id="@+id/ivFav"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="@dimen/favorite_vehicle_fav_icon_padding"
                android:src="@drawable/ic_favorite_fill"
                android:tint="@color/primary_green_dark"
                tools:ignore="UseAppTint" />

        </com.google.android.material.card.MaterialCardView>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
